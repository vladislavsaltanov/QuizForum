# QuizForum

## Agent skills

### Issue tracker

Issues live as local markdown under `.scratch/<feature>/`, mirrored to GitHub Issues via `gh` (remote: `vladislavsaltanov/QuizForum`). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
